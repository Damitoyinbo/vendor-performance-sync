# vendor-performance-sync
Laravel app that installs into a Shopify store via OAuth, pulls the product/variant catalog through the Admin GraphQL API, and aggregates a per-vendor inventory performance dashboard.
